[phases.setup]
nixPkgs = ["go_1_25"]

[phases.install]
cmds = [
  "npm ci --omit=dev",
  "GOPATH=/app/.go go install github.com/steipete/wacli/cmd/wacli@latest",
  "cp /app/.go/bin/wacli /usr/local/bin/wacli"
]

[start]
cmd = "node src/index.js"
